# ─────────────────────────────────────────────────────────────
# TraveLand Backend – Environment Variables Reference
# Copy this to .env and fill in your values.
# ─────────────────────────────────────────────────────────────

# Server
PORT=5000
NODE_ENV=development       # Use 'production' on Railway

# ── Local Dev (XAMPP MySQL) ───────────────────────────────────
# Used when DB_HOST / etc. are set (no Railway MySQL plugin).
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=               # Leave blank for XAMPP default
DB_NAME=traveland_db

# ── Railway MySQL Plugin ──────────────────────────────────────
# These are AUTO-INJECTED by Railway when you add the MySQL plugin.
# You do NOT need to set these manually in the Railway dashboard.
#   MYSQLHOST=...
#   MYSQLPORT=...
#   MYSQLUSER=...
#   MYSQLPASSWORD=...
#   MYSQLDATABASE=...

# ── JWT ───────────────────────────────────────────────────────
JWT_SECRET=your_super_secret_jwt_key_here_change_this
JWT_EXPIRES_IN=7d

# ── CORS ──────────────────────────────────────────────────────
# Comma-separated list of allowed frontend origins.
# Local: http://localhost:5173
# Railway: https://your-frontend-domain.com  (if hosted elsewhere)
CLIENT_URL=http://localhost:5173
